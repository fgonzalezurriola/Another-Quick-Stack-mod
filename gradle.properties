# Plugin version (semantic versioning)
version=1.0

# Maven group for publishing
maven_group=dev.fgonz

# Java version - Hytale runs on Java 25
java_version=25

# Include asset pack (UI files, etc.)
includes_pack=true

# Release channel (release or pre-release)
patchline=release
