# Another Quick Stack Mod

A command that quickly stacks items into nearby chests.

It does not require /op permissions → available in Adventure mode.

Commands:

`/qsu` → Fast stacking without a menu

`/quickstack` → Menu version (includes configurable **backpack support** and radius)
